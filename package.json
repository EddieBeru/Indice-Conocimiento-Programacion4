{
  "name": "tarea-continua",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npx esbuild public/game/main.js --bundle --outfile=public/game/bundle.js && npx esbuild public/game-dom/main.js --bundle --outfile=public/game-dom/bundle.js && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.1.5"
  },
  "devDependencies": {
    "astro-relative-links": "^0.4.2"
  }
}
